const inventoryItems = new Map(); // id -> { name, quantity }
module.exports = inventoryItems;
//A cache is a temporary storage area that stores frequently accessed data so that future requests for that data can be served faster.
